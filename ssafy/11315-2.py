def check(row,col):
    for d in range(4):
        while 각 방향의 값 'o'인 동안:
            cnt+=1
        if cnt >=5
            return True
        return False

TC=int(input())
for test_case in range(1,TC+1):
    N=int(input())
    arr=[input() for i in range(N)]
    for row in range(N):
        for col in range(N):
            if arr[row][col]=='o':
                check(row, col)